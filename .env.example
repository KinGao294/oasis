# Copy to `.env` and fill in values. Do not commit `.env`.
# For local shells: `set -a && source .env && set +a` (bash) or use `export VAR=value`.

# Zhipu GLM (智谱) — required by `scripts/summarize.py` for AI summaries.
# Obtain a key from https://open.bigmodel.cn/
ZHIPU_API_KEY=
